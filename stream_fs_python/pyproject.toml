[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "sfs"
version = "0.1.0"
description = "Python bindings for the Stream File System library"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
